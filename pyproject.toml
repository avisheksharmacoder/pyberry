[build-system]
requires = ["setuptools>=42", "Cython>=3.0.0", "setuptools-rust"]
build-backend = "setuptools.build_meta"

[project]
name = "pyberry-framework"
version = "0.1.8"
dynamic = ["description", "readme", "authors"]
dependencies = [
    "granian",
    "cython",
    "setuptools",
    "libsql-client",
    "setuptools-rust"
]

[project.scripts]
pyberry = "pyberry.cli:main"
